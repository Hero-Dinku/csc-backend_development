<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./layouts/head', { title: title }) %>
    <style>
        .hero {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 100px 0;
            text-align: center;
        }
        .hero h1 {
            font-size: 3.5rem;
            margin-bottom: 1rem;
            font-weight: 700;
        }
        .hero p {
            font-size: 1.3rem;
            margin-bottom: 2rem;
            opacity: 0.9;
        }
        .features {
            padding: 80px 0;
        }
        .feature-card {
            text-align: center;
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            margin-bottom: 2rem;
            transition: transform 0.3s ease;
            background: white;
        }
        .feature-card:hover {
            transform: translateY(-5px);
        }
        .feature-icon {
            font-size: 3rem;
            margin-bottom: 1rem;
            color: #667eea;
        }
        .cta-section {
            background: #f8f9fa;
            padding: 80px 0;
            text-align: center;
        }
    </style>
</head>
<body>
    <%- include('./partials/header') %>
    
    <section class="hero">
        <div class="container">
            <h1>Premium Vehicle Management</h1>
            <p class="lead">Experience the future of vehicle inventory management with our cutting-edge platform</p>
            <% if (!loggedin) { %>
                <div class="hero-buttons">
                    <a href="/account/login" class="btn btn-light btn-lg me-3">Sign In</a>
                    <a href="/account/register" class="btn btn-outline-light btn-lg">Get Started - Register Free</a>
                </div>
            <% } else { %>
                <div class="hero-buttons">
                    <a href="/account/management" class="btn btn-light btn-lg me-3">Manage Account</a>
                    <% if (account && (account.account_type === 'Employee' || account.account_type === 'Admin')) { %>
                        <a href="/inv" class="btn btn-outline-light btn-lg">Inventory Management</a>
                    <% } %>
                </div>
            <% } %>
        </div>
    </section>

    <section class="features">
        <div class="container">
            <div class="row text-center mb-5">
                <div class="col">
                    <h2>Why Choose Our Platform?</h2>
                    <p class="lead">Powerful features designed for modern vehicle management</p>
                </div>
            </div>
            <div class="row">
                <div class="col-md-4">
                    <div class="feature-card">
                        <div class="feature-icon">🚗</div>
                        <h3>Smart Inventory</h3>
                        <p>Manage your vehicle inventory with intelligent categorization and advanced search capabilities.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="feature-card">
                        <div class="feature-icon">🔒</div>
                        <h3>Secure Access</h3>
                        <p>Role-based authentication ensures your data remains secure and accessible only to authorized personnel.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="feature-card">
                        <div class="feature-icon">📊</div>
                        <h3>Real-time Analytics</h3>
                        <p>Get insights into your inventory performance with comprehensive analytics and reporting.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="cta-section">
        <div class="container">
            <h2>Ready to Transform Your Vehicle Management?</h2>
            <p class="lead mb-4">Join thousands of satisfied users managing their vehicle inventory efficiently</p>
            <% if (!loggedin) { %>
                <a href="/account/register" class="btn btn-primary btn-lg me-3">Start Your Journey Today</a>
                <a href="/account/login" class="btn btn-outline-primary btn-lg">Already have an account?</a>
            <% } else { %>
                <a href="/account/management" class="btn btn-primary btn-lg">Go to Dashboard</a>
            <% } %>
        </div>
    </section>

    <%- include('./partials/footer') %>
</body>
</html>
