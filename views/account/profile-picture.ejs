<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        body { font-family: Arial, sans-serif; max-width: 600px; margin: 50px auto; padding: 20px; }
        .header { background: #333; color: white; padding: 1rem; margin-bottom: 2rem; }
        .nav a { color: white; margin-right: 1rem; text-decoration: none; }
        .error { background: #f8d7da; color: #721c24; padding: 10px; border-radius: 4px; margin: 1rem 0; }
        .success { background: #d4edda; color: #155724; padding: 10px; border-radius: 4px; margin: 1rem 0; }
        .profile-container { text-align: center; }
        .current-picture { width: 150px; height: 150px; border-radius: 50%; object-fit: cover; border: 3px solid #007bff; margin: 20px 0; }
        .upload-form { margin: 2rem 0; }
        .file-input { margin: 1rem 0; }
        .btn { padding: 10px 20px; background: #007bff; color: white; border: none; border-radius: 4px; cursor: pointer; margin: 5px; }
        .btn-secondary { background: #6c757d; }
    </style>
</head>
<body>
    <div class="header">
        <nav class="nav">
            <a href="/">Home</a>
            <a href="/account/management">Account Management</a>
            <a href="/logout">Logout</a>
            <span style="float: right;">Welcome, <%= user.account_firstname %>!</span>
        </nav>
    </div>

    <h1>Update Profile Picture</h1>
    
    <% if (error) { %>
        <div class="error"><%= error %></div>
    <% } %>
    
    <% if (success) { %>
        <div class="success"><%= success %></div>
    <% } %>
    
    <div class="profile-container">
        <% if (user.profile_picture_url) { %>
            <h3>Current Profile Picture:</h3>
            <img src="<%= user.profile_picture_url %>" alt="Profile Picture" class="current-picture">
        <% } else { %>
            <p>No profile picture set yet.</p>
            <div style="width: 150px; height: 150px; border-radius: 50%; background: #ddd; margin: 20px auto;"></div>
        <% } %>
        
        <div class="upload-form">
            <h3>Upload New Picture</h3>
            <form action="/account/upload-profile-picture" method="POST" enctype="multipart/form-data">
                <div class="file-input">
                    <input type="file" name="profilePicture" accept="image/*" required>
                </div>
                
                <button type="submit" class="btn">Upload Picture</button>
                <a href="/account/management">
                    <button type="button" class="btn btn-secondary">Back to Account</button>
                </a>
            </form>
        </div>
    </div>
</body>
</html>
